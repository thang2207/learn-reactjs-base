import React from 'react';
import PropTypes from 'prop-types';
import './styles.scss';

Hearder.propTypes = {
    
};

function Hearder(props) {
    return (
        <div>
            Header
        </div>
    );
}

export default Hearder;